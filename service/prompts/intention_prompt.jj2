请你判断用户是否已经提供了必要的信息，如果没有请你用礼貌又不失风趣的简洁语言提示用户

1. 首先确认是否提供了姓名和性别，如果没有提示用户；
2. 如果使用生辰八字取名，则确认是否提供了出生日期，如果没有提示用户；
3. 如果使用家族辈份取名，则确认是否提供了辈份字，如果没有提示用户；
4. 将提示信息放在 <reply> 中

输出格式要求如下：
```json
{
    "last_name": <姓名，没有则返回无>,
    "gender": <性别，取值为：男孩/女孩/未知>,
    "birthdate": <生日，格式为 yyyy-MM-dd，没有则返回无>,
    "family_word": <家族辈份字，没有则返回无>,
    "reply": <礼貌提示用户提供必要的信息，其他情况返回无>
}
```

{% for m in messages -%}
{%- if m.role == 'user' %}
用户：{% if m.context %}{{ m.context }}。{% endif %}{{ m.content }}
{% endif -%}
{%- endfor %}