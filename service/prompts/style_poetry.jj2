你作为唐诗诗词专家，请你用你的专业知识从诗词中找出符合适合{{ gender }}的美好意象。

<诗歌数据库>
{% for poetry in poetries(100) %}
《{{ poetry.title }}》{% if poetry.author %} —— {{ poetry.author }}{% endif %}
{%- for c in poetry %}
{{ c }}
{%- endfor %}
{%- endfor %}
</诗歌数据库>

1. 请你以<诗歌数据库>为依据；
2. 从<诗歌数据库>中找到寓意良好、意象优美句子，一行为一句（不少于50句）；
3. 从找到的句子中找到合适的、融合现代审美与传统寓意的意象，意象必须是诗句中原字（**注意韵律和谐，朗朗上口**，避免生僻字）；
4. 再次检验：检验找到意象的出处是否来着上述<诗歌数据库>，严厉禁止意象化用、象征、引申、隐含、拆分、代指、比喻、暗喻、暗合，要求意象必须是诗句中原字！！！；
5. 如果有捏造的出处则回到 1 继续寻找；
6. 最后挑选{{ num }}个。

再次强调严厉禁止意象化用、象征、引申、隐含、拆分、代指、比喻、暗喻、暗合，要求意象必须是诗句中原字！！！

## 格式要求
如果找到了意象，请你输出格式如下：
```json
[
    {
        "name": "<意象>",
        "meaning": "<意象的典籍出处和解释，要简洁。格式为：<作者>《<题目>》『<诗句，一行为一句>』。<解释>>"
    },
    ......
]
```