你作为唐诗诗词专家，请你用你的专业知识从诗词中找出符合适合{{ gender }}的美好意象。

<诗歌数据库>
{% for poetry in poetries(50) %}
《{{ poetry.title }}》 —— {{ poetry.author }}
{%- for c in poetry %}
{{ c }}
{%- endfor %}
{%- endfor %}
</诗歌数据库>

1. 请你以<诗歌>为依据；
2. 从<诗歌>中找到寓意良好、意象优美句子（不少于50句）；
3. 从找到的句子中找到合适的意象（**注意韵律和谐，朗朗上口**，避免生僻字）；
4. 再次检验：检验生成的名字的出处是否来着上述<诗歌>；
5. 如果有捏造的出处则回到 1 继续寻找合适的意象；
6. 最后挑选{{ num }}个意象。

## 格式要求
如果找到了意象，请你输出格式如下：
```json
[
    {
        "name": "<意象>",
        "pinyin": "<拼音>",
        "meaning": "<意象的典籍出处和引用典籍的解释>"
    },
    ......
]
```